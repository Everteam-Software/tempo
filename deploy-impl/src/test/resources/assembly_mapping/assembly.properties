#
# Assembly descriptor
#
# Each entry is a mapping between a component directory and its target container
# e.g. myProcess=ApacheOde

# Test mapping from MappedEngine -> MockEngine
component1=MappedEngine